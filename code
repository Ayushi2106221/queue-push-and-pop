queue<int>_push(int arr[],int n)
{
   queue<int>q;
  int front=1, rear=1;
  for(int i=0;i<n;i++){
      q.push(arr[i]);
      rear++;
  }
  return q;
}

void _pop(queue<int>q)
{
   while(!q.empty()){
       cout<<q.front()<<" ";
       q.pop();
   }
} 
